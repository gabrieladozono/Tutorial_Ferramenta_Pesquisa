# 🧠 Tutorial: Como Rodar um fluxo com ferramente de pesquisa no Langflow

Este tutorial explica **passo a passo** como rodar um **fluxo de código** dentro do **Langflow**, desde a instalação até a execução completa.  
O objetivo é ajudar usuários a entenderem como **abrir e testar fluxos** no ambiente Langflow de forma prática.

---

## 1. Baixando o Langflow

Antes de começar, é necessário ter o **Langflow** instalado no seu computador. É possível baixar por lojas de aplicativos, site do Langflow ou por via pip.
- Para baixar via pip, se você já tem o **Python 3.9+** instalado, rode no terminal:
  
```bash
pip install langflow
```
- Depois de instalado, execute o comando abaixo para iniciar o Langflow localmente:

```bash
langflow run
```
## 2. Abrindo um fluxo do tipo Basic Prompting

Com o Langflow aberto:
Clique em **“New Flow”** no canto superior direito.
Na janela de modelos, selecione o template chamado “Basic Prompting”.


